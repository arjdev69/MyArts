import React from 'react';

import {View} from 'react-native';
import {Text} from 'container/Login/Form/styles';

import {Background} from 'components';

const Cart: React.FC = () => {
  return (
    <View style={{height: '100%', width: '100%'}}>
      <Background>
        <Text>Teste</Text>
      </Background>
    </View>
  );
};

export default Cart;
